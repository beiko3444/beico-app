// Create a JS file that executes the TS route handler using tsx or ts-node
// Since we don't have it easily set up, let's use a Next.js API direct call
const { POST } = require('../app/api/auth/signup/route.ts');
